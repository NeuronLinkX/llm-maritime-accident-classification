# STEP **1** **전처리 개발 리포트**

## PDF **종류** **분류**

**레이아웃 종류** (`pdf_layout_type`, 2종)

| **종류**         | **설명**                                                                                             |
| ---------------------- | ---------------------------------------------------------------------------------------------------------- |
| `portrait_or_single` | 세로형**단일** **페이지**                                                                      |
| `landscape_two_up`   | 가로 폭이 세로의 1.2배 이상<br />스캔된 두 페이지가 한 이미지에 좌우로 나란히**붙은** **경우** |

**추출 방식** (`extraction_mode`, 3단계 계층)

| **순서** | **모드**                | **설명**                                                                 |
| -------------- | ----------------------------- | ------------------------------------------------------------------------------ |
| 1              | `pdf_native_to_hwpx_cpp`    | 임베디드 텍스트 레이어를`pdftotext`로 직접 추출 (가장 빠르고 **정확)** |
| 2              | `pdf_paddleocr_to_hwpx_cpp` | 1차 로컬 OCR(GPU) : native 품질**미달** **시**                     |
| 3              | `pdf_ocrmypdf_to_hwpx_cpp`  | Tesseract 기반 2차 폴백 : PaddleOCR 불가**시에만**                       |

---

## 라벨/별칭 문제 전체 카탈로그

### PDF 파이프라인(Python)

1. **"사건 개요" 별칭 자체가 틀림**
   1. 처음엔 "판시요지 가./나." 서술형 패턴만 찾도록 만들었는데, 실제 문서 대부분에서 "사건 개요"가 가리키는 건 그 문단이 아니라 문서 상단 **"관련선박" 표**였음.
   2. "판시요지" 대신 "판시사항"으로 표기되거나, 요약문이 "~사안"/"~사건"이 아니라 "~발생함"으로 끝나 서술형 패턴 자체가 안 맞는 경우도 많았으므로 "관련선박" 표 기반 추출 경로 추가함
2. **경계 마커 과매칭**
   1. "사고 경위"/"장소" 같은 라벨이 문장 **어디서나** 매칭되어 "위 사고일시와 **장소**에서"의 "장소", "위와 같은 **이유**로"의 "이유"에서 필드가 잘못 잘림 따라서, 라벨이 문장  **맨 앞(offset 0)** 에서만 절 경계로 인정하도록 제한
3. **표의 "사건개요/일시/장소" 라벨이 "사고경위" 옆/아래에 겹쳐 인식**
   1. 컬럼 레이아웃 OCR에서 "사고경위" 다음 줄에 "사 건개요"만 덩그러니 찍히는 경우, 이걸 진짜 서술문 시작으로 착각하면 "사고 경위: 사건개요 ..." 형태로 오염돼 C++ 쪽에서 "사건개요" 라벨로 오인되어 통째로 잘림
   2. 따라서, `SIBLING_BARE_LABEL_RE`로 걸러냄
4. **OCR 중간 띄어쓰기로 라벨 자체가 안 깨짐**
   1. "판시사항" → "판 시 사 항"처럼 글자 사이에 공백이 끼어들어 단순 문자열 매칭이 실패함
   2. 따라서, 글자 사이 임의 공백 허용하는 `_spaced_pattern()` 도입
5. **날짜(일시) 표기 흔들림 3종**
   1. "13.:30"(콜론 앞 마침표 오삽입), "55경"(분 단위자 "분" 누락), "2021년 3월 2일 06:30경"(연월일+콜론 혼합 표기)
   2. 처음엔 패턴 3개를 하드코딩으로 하나씩 추가하다가, 나중에 "연/월/일/시/분 숫자 5개, 사이 구분자는 뭐든 허용"하는 관대한 패턴 하나로 통합
6. **"장소" 필드에 다른 라벨이 컬럼 병합으로 끼어듦**
7. `	PLACE_SIBLING_LABEL_RE`로 "사건개요"/"사고경위" 잔재 제거
8. **PaddleOCR 언어모델 설정 버그**
   1. (라벨 문제는 아니지만 원인) : `text_detection_model_name`을 명시하면 `lang="korean"`이 인식(rec) 모델에도 무시돼 중국어처럼 깨진 텍스트가 나옴
   2. 따라서, det/rec 모델명을 항상 같이 명시

### HWP/HWPX native 경로(C++)

8. **"일시" 값이 순수 숫자만 있으면 "의미 없는 조각"으로 버려짐**
   1. `is_meaningful_chunk()`가 "값에 한글이 최소 1글자는 있어야 유효"라는 기준을 썼는데, 정상 케이스는 "2020. 10. 4. 16:20 **경** "처럼 끝에 "경"(한글)이 붙어서 통과됐던 것뿐,
   2. "2023. 12. 23. 09:19"처럼 "경" 없이 순수 숫자로만 적힌 문서는 한글이 하나도 없어 통째로 버려짐
   3. 따라서, "일시" 필드에 한해 숫자 6자리 이상이면 한글 없어도 인정하도록 예외 추가
9. **"장소" 라벨이 "사고장소"(합성어)로 쓰이면 별칭 목록에 없어서 못 찾음**
   1. `keyword_rules()`에 "장소"/"일시" 단독형만 등록돼 있었고, "사고장소"/"사고일시" 합성어 표기는 별도 별칭이 없었음
   2. 따라서, 별칭에 "사고일시"/"사고장소" 추가
      1. **주의** : 이 별칭은 완전히 다른 문서 양식("전체 재결서")에서도 똑같이 쓰이는데, 그 경우엔 "사건개요"/"관련선박"/"사고경위" 라벨 자체가 원문에 없어서 그 필드들은 여전히 비게 됨(의도된 동작 — 일시/장소만 건지고 나머지는 검토 필요로 정직하게 남김)
10. **"해양사고관련자"가 절 표지이자 문장 주어로 이중으로 쓰임**
    1. 이 단어는 (a) 문서 앞부분 "관련자 목록" 절 헤더로도 쓰이고, (b) 사고경위 서술문에서 "해양사고관련자 이 선박 선장 A가 ~~~"처럼 그냥 **문장의 주어**로도 흔히 쓰임.
    2. 문장 맨 앞에 오면 무조건 절 경계로 인식하는 규칙 때문에 (b)의 경우 사고경위 수집이 첫 문장부터 통째로 끊겨버림
    3. → "해양사고관련자"는 뒤에 아무 내용도 안 이어지고 그 단어 자체로 유닛이 끝날 때만(=진짜 헤더처럼 단독 줄일 때만) 절 경계로 인정하도록 수정
11. **"주문" 라벨의 이중성** (앞서 인지, 별도 처리)
    1. "주문"이 실제 본문 문장 앞머리에 컬럼 병합으로 붙는 경우(예: "주문 쪽 안전핀이 부러지면서...")가 있어, 무조건 절 헤더로 취급하지 않고 접두어만 제거한 뒤 수집을 계속하도록(`strip_margin_label`) 별도 처리해둠
12. **표준 라벨 자체가 없는 문서 양식 ("전체 재결서")**
    1. 라벨 표기 문제가 아니라  **문서 구조 자체가 다름** .
    2. "재결요약서"(관련선박 표 + 일시/장소/사고경위 4필드)가 아니라 "재결서" 원문(이유/사실/주문 구조, "나. 사실의 경과" 같은 다른 절 이름)이 섞여 들어오면 "사건개요"/"관련선박"/"사고경위" 라벨이 원문에 아예 없어서 애초에 매칭할 대상이 없음.
    3. 코드로 "고칠" 수 없는 유형으로 판단하였으므로, `format_mismatch` 플래그로 정확히 표시만 함. (3건이 존재함: 제609천금호, 제2021신영호, 어선호)
